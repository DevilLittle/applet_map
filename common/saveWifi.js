/**
 * 保存WIFI
 */
const addWifi = (list)=>{
    
}

/**
 * 保存WIFI记录
 * {
 * time:时间戳,
 * data:数据
 * }
 */
const reportWifi = ()=>{
    let data = {
        'time': timestamp,
        'data': this.data.wifiArray
    }

    wifiRecord.push(data);
}